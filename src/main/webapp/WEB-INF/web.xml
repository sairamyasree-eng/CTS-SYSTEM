<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
	version="6.0">
	<!-- BUG-FIX: Eagerly initialise Hibernate at startup so tables are created 
		(hbm2ddl.auto=update) before any user request arrives. -->
	<listener>
		<listener-class>com.iispl.util.HibernateInitListener</listener-class>
	</listener>
	<!-- ZK Loader -->
	<servlet>
		<servlet-name>zkLoader</servlet-name>
		<servlet-class>org.zkoss.zk.ui.http.DHtmlLayoutServlet</servlet-class>
		<init-param>
			<param-name>update-uri</param-name>
			<param-value>/zkau</param-value>
		</init-param>
		<load-on-startup>1</load-on-startup>
	</servlet>
	<servlet-mapping>
		<servlet-name>zkLoader</servlet-name>
		<url-pattern>*.zul</url-pattern>
	</servlet-mapping>
	<!-- ZK AU Engine -->
	<servlet>
		<servlet-name>auEngine</servlet-name>
		<servlet-class>org.zkoss.zk.au.http.DHtmlUpdateServlet</servlet-class>
	</servlet>
	<servlet-mapping>
		<servlet-name>auEngine</servlet-name>
		<url-pattern>/zkau/*</url-pattern>
	</servlet-mapping>
	<!-- Welcome Page -->
	<welcome-file-list>
		<welcome-file>login.zul</welcome-file>
	</welcome-file-list>
</web-app>